<?php	 	
# Proxy description text
$themeReplace['proxy_description'] = <<<OUT
<p>SocialStrainer Proxy</p>
<p>Las URLs que introduzcas ser&aacute;n pasadas por un proxy (espec&iacute;ficamente el servidor que est&aacute; sirviendo este sitio), haz un uso consciente de &eacute;ste y s&oacute;lo util&iacute;zalo cuando sea necesario porque el ancho de banda es limitado y podr&iacute;as estar consumiendo recursos que otros necesiten</p>
<p>Nuestra recomendaci&oacute;n es que uses el proxy para introducir URLs de im&aacute;genes</p>
OUT;

# Ad location above the form on the index page
$themeReplace['index_above_form'] = <<<OUT

OUT;

# Ad location below the form on the index page
$themeReplace['index_below_form'] = <<<OUT

OUT;

# Ad location on proxified pages below the url mini-form
$themeReplace['proxified'] = <<<OUT

OUT;

?>